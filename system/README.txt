These are really just plugins, but they are "system" plugins D:
They basically make it work.

They get first dibs on the messages so they can handle them before plugins can.
Needed to make sure that user data is updated before the plugins get their hands on it.